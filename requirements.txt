# Core libraries
streamlit==1.33.0
selenium==4.20.0
beautifulsoup4==4.12.3

# Email notifications
email-validator==2.1.0.post1

# Web scraping helpers (optional but useful)
lxml==5.2.1

# JSON comparison (for change detection)
deepdiff==6.7.1

# Optional: for colored terminal logs or rich formatting
rich==13.7.1

# Optional: for scheduling locally (not required in GitHub Actions)
schedule==1.2.1
